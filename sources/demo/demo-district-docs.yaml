key: demo/demo-district-docs
name: "DemoDistrict decision documents + scans (SYNTHETIC FIXTURE)"
description: >
  Synthetic approval decisions (PDF) and a fake scanned map (PNG) plus the
  human-georeferenced COG. Feeds document parsing and the georeferencing demo.
authority: "UBND Tỉnh Mẫu (SYNTHETIC)"
jurisdiction: "DemoDistrict"
admin_codes:
  district: "SYNTH-DEMO-01"
source_type: file
base_url: "file://fixtures/synthetic/demo_district"
discovery:
  path: fixtures/synthetic/demo_district
  globs: ["documents/*.pdf", "scans/*.png", "georeferenced/*.tif"]
parser:
  # no default_derivation_level: PDFs become Documents (no level);
  # the COG is auto-classified official_raster, the unreferenced PNG scan
  # becomes derived_machine_unreviewed + a review task — exactly the demo flow.
  default_dataset_group: other
planning:
  title: "Quy hoạch phân khu DemoDistrict (SYNTHETIC)"
  planning_type: quy_hoach_phan_khu
  scale: "1/2000"
  approving_authority: "UBND Tỉnh Mẫu (SYNTHETIC)"
  version_label: "unversioned-docs"
rights:
  license: "CC0-1.0"
  rights_statement: "Synthetic test fixture generated by OpenQuyHoach — no real-world meaning."
  redistribution_status: allowed
enabled: true
